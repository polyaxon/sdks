/**
 * Polyaxon SDKs and REST API specification.
 *    
 *
 * The version of the OpenAPI document: 2.16.0
 * Contact: contact@polyaxon.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The V1CreatePtyRequest model module.
 * @module model/V1CreatePtyRequest
 * @version 2.16.0
 */
class V1CreatePtyRequest {
    /**
     * Constructs a new <code>V1CreatePtyRequest</code>.
     * Request body for POST /pty.
     * @alias module:model/V1CreatePtyRequest
     */
    constructor() { 
        
        V1CreatePtyRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>V1CreatePtyRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/V1CreatePtyRequest} obj Optional instance to populate.
     * @return {module:model/V1CreatePtyRequest} The populated <code>V1CreatePtyRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new V1CreatePtyRequest();

            if (data.hasOwnProperty('command')) {
                obj['command'] = ApiClient.convertToType(data['command'], ['String']);
            }
            if (data.hasOwnProperty('env')) {
                obj['env'] = ApiClient.convertToType(data['env'], Object);
            }
            if (data.hasOwnProperty('workdir')) {
                obj['workdir'] = ApiClient.convertToType(data['workdir'], 'String');
            }
            if (data.hasOwnProperty('cols')) {
                obj['cols'] = ApiClient.convertToType(data['cols'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], 'Number');
            }
            if (data.hasOwnProperty('tag')) {
                obj['tag'] = ApiClient.convertToType(data['tag'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>V1CreatePtyRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>V1CreatePtyRequest</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['command'])) {
            throw new Error("Expected the field `command` to be an array in the JSON data but got " + data['command']);
        }
        // ensure the json data is a string
        if (data['workdir'] && !(typeof data['workdir'] === 'string' || data['workdir'] instanceof String)) {
            throw new Error("Expected the field `workdir` to be a primitive type in the JSON string but got " + data['workdir']);
        }
        // ensure the json data is a string
        if (data['tag'] && !(typeof data['tag'] === 'string' || data['tag'] instanceof String)) {
            throw new Error("Expected the field `tag` to be a primitive type in the JSON string but got " + data['tag']);
        }

        return true;
    }


}



/**
 * Command argv. Empty means use the server default shell.
 * @member {Array.<String>} command
 */
V1CreatePtyRequest.prototype['command'] = undefined;

/**
 * Environment overrides. JSON null means unset the key.
 * @member {Object} env
 */
V1CreatePtyRequest.prototype['env'] = undefined;

/**
 * Working directory.
 * @member {String} workdir
 */
V1CreatePtyRequest.prototype['workdir'] = undefined;

/**
 * Terminal columns.
 * @member {Number} cols
 */
V1CreatePtyRequest.prototype['cols'] = undefined;

/**
 * Terminal rows.
 * @member {Number} rows
 */
V1CreatePtyRequest.prototype['rows'] = undefined;

/**
 * Optional reconnect handle.
 * @member {String} tag
 */
V1CreatePtyRequest.prototype['tag'] = undefined;






export default V1CreatePtyRequest;

