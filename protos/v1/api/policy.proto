syntax = "proto3";

package v1;

import "google/protobuf/struct.proto";
import "google/protobuf/timestamp.proto";

import "v1/api/user_access.proto";

option go_package = "v1/api";

// Organization policy
message Policy {
  // UUID
  string uuid = 1;

  // Owner/namespace where the project was created
  string owner = 2;

  // Required name
  string name = 3;

  // Optional description
  string description = 4;

  // Optional name of user created this entity
  string user = 5;

  // Optional tags of this entity
  repeated string tags = 6;

  // Optional time when the entity was created
  google.protobuf.Timestamp created_at = 7;

  // Optional last time the entity was updated
  google.protobuf.Timestamp updated_at = 8;

  // Optional project excluded features
  repeated string excluded_features = 9;

  // Optional project excluded runtimes
  repeated string excluded_runtimes = 10;

  // Optional project archived deletion interval
  int32 archived_deletion_interval = 11;

  // Authorized connections
  repeated string connections = 12;

  // Default Presets
  repeated string default_presets = 13;

  // Default Presets ordered
  repeated string default_presets_ordered = 14;

  // Authorized Presets
  repeated string presets = 15;

  // Default Queue
  string queue = 16;

  // Authorized queues
  repeated string queues = 17;

  // Authorized agents
  repeated string agents = 18;

  // Authorized namespaces
  repeated string namespaces = 19;

  // Authorized users
  repeated UserAccess user_accesses = 20;

  // Authorized teams
  repeated string teams = 21;

  // Authorized projects
  repeated string projects = 22;

  // Connected projects
  repeated string connected_projects = 23;
}


 // Request data to create/update policy
 message PolicyBodyRequest {
  // Owner of the namespace
  string owner = 1;

  // Policy body
  Policy policy = 2;
}


// Contains list of policies
message ListPoliciesResponse {
  // Count of the entities
  int32 count = 1;

  // List of all entities
  repeated Policy results = 2;

  // Previous page
  string previous = 3;

  // Next page
  string next = 4;
}
