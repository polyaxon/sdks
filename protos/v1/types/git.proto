syntax = "proto3";

package v1;

option go_package = "v1/types";

// Git type specification, url or a connection is required
message GitType {
    // Optional Url
    string url = 1;

    // Optional revision
    string revision = 2;

    // Addional command flag
    repeated string flags = 3;
}
