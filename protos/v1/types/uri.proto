syntax = "proto3";

package v1;

option go_package = "v1/types";

// Uri type specification
message UriType {
    // Optional user
    string user = 1;

    // Optional password
    string password = 2;

    // Optional host
    bool host = 3;
}
