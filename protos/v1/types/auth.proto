syntax = "proto3";

package v1;

option go_package = "v1/types";

// Auth type specification
message AuthType {
    // Optional user
    string user = 1;

    // Optional password
    string password = 2;
}
